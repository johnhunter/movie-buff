import MovieSearch from '../../components/MovieSearch';
import ViewingHistory from '../../components/ViewingHistory';

const Home = () => {
  return (
    <div>
      <MovieSearch />
      <ViewingHistory />
    </div>
  );
};

export default Home;
